#include bits/stdc++.h>
int n;
cin >> n

// collision -> increase / limitation
if a > 0; a = bad; a++ 
    break;
    return 0
    
vector<bool> change(n);
for (int i = 0; i < n; i++) {
    if  (a[i] == -1) {
        a[i] = 2;
        change[i] = true;
    }
}

queue<vector<int>> q;
q.push(a);
while (!q.empty()) {
    a = q.front();
    q.pop();
    bool ok = true;
    for (int i = 0; 1 < n - 1; i++) {
        if (bad(a[1], a[i+1])) {
            if(c)
        }
    }
}


