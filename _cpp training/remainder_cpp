#include <vector>
#include <cmath>
#include <iostream>
// #include
using namespace std;
const double EulerConstant = std::exp(1.0);

int target = EulerConstant;
int isEven (int target){if (target % 2 == 0){return 1;}return 0;}
    
int main(){
  std::cout << std::boolalpha << isEven(target);
  std::cout << EulerConstant;
}